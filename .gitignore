# Self-update temporary download (left behind only if interrupted)
.hostctl-update.*

# Logs, if the script is ever run from inside the repo directory
*.log

# Editor / OS noise
*~
*.swp
*.swo
.DS_Store
Thumbs.db
